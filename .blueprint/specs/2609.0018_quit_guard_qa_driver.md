# 2609.0018 Drive the ⌘Q guard from the QA driver

**Date:** 2026-09-19
**Status:** draft
**Author:** EP Lin
**Issue:** [#183](https://github.com/pingeplin/tarmac/issues/183) `feat(dev): drive the ⌘Q guard from the QA driver`
**Builds on:** [2609.0015](2609.0015_in_app_qa_driver.md) (the `tarmac dev` driver), [2609.0016](2609.0016_hold_to_quit.md) (the ⌘Q guard, amended by #179)
**Base branch:** `refactor/179-quit-poll-run-loop-timer` (#179, PR #182): the timer poller is where the hold override plugs in.

## Context

The ⌘Q guard is the native Quit item retargeted onto `tarmacQuit:`
(`desktop/src-tauri/src/quit_intercept.rs`). A keyboard ⌘Q shows the notice. A
~500 ms hold hides the window and quits on release, and two taps within 1 s
quit. Release is read by a 50 ms main-run-loop timer that polls
`CGEventSource::key_state(CombinedSessionState, key)`.

Every change to that path owes Q1, Q11, Q12 and Q16 of
`desktop/qa/hold-to-quit-qa.md` by hand (the rows #179 names). The driver cannot
help today:
- `tarmac dev key` dispatches DOM events, which never reach AppKit;
- it refuses ⌘ chords;
- `make qa`'s only guard check, D11, reads whether the item is retargeted and
  presses nothing.

**Measured before writing this spec.** Two spikes ran on 2026-09-19. Their
tools and logs are in the main checkout's `.dev/`, which is gitignored and
machine-local.

1. **Out of process, `CGEventPostToPid`** (`.dev/postpid-spike/`, against a
   `main` dev app).
   - Posted ⌘Q taps reach `tarmacQuit:` (`route=guard`, `age_ms` 6–65). This
     works whether or not the app is frontmost.
   - A double tap quits. Q16's re-show works. A keyDown stamped now − 2500 ms
     routes `terminate`.
   - **No event source ever makes `key_state` read a posted key as down.** A
     posted 2 s hold ended as a tap (`t3b-nil.log`).
   - Posted keys go through the active input source. With a Zhuyin
     composition pending, a posted ⌘Q did nothing.
   - `CGEventSourceKeyState(.privateState, …)` deadlocked inside SkyLight.
     This was observed during the spike, but no log of it survived:
     `t3-private.log` records a *posted* private-source event, not the query.
2. **In process, `NSEvent` + `[NSApp postEvent:atStart:NO]`**
   (`.dev/inapp-spike-183/`, against this branch at `ec07e96`).
   - A constructed ⌘Q keyDown posted on the main thread reaches `tarmacQuit:`.
     `[NSApp currentEvent]` is the posted object, with the timestamp and flags
     it was built with.
   - **It takes the real path only while the main window is key.** Then the
     handler's backtrace has 9 WebKit frames
     (`WebPageProxy::didReceiveEvent → doneWithKeyEvent → … →
     -[NSMenu performKeyEquivalent:]`). With the window not key, the event goes
     straight to the menu with 0 WebKit frames, and the page never sees it.
   - **Knockout proof.** Kitty flags 5 were staged with `printf '\033[>5u'`.
     - With the #171 kitty rule in `kit/termKeyRoute.ts`, the ⌘Q routed
       `guard`.
     - With the rule knocked out, it produced no `quit-key` line
       (`run1.log`, 120845319 → 120908626) and no notice
       (`a3-knockout-watch.log`).
     - With the rule restored, it routed `guard` again.

     Two observations from the run are not in its saved logs. The spike
     reported both. First, the flags were confirmed in-band (`?5u`). Second,
     under the knockout the prompt showed `3;9u`: the tail of xterm's
     `ESC[113;9u`, the encoding #171's harness recorded in the main checkout's
     `.dev/key-harness/quit171/run2.log`.
   - **Focus contexts.** A plain-shell terminal, the board, a markdown card and
     an HTML card all routed `guard` through the page. The two doc cards were
     focused by posted mouse clicks, because `tarmac dev focus` refuses doc
     cards.
   - **Activation.** The measured posture is narrow. The dev app was hidden
     with `hide`, unhidden with `unhideWithoutActivation`, and another app was
     left frontmost by the system. From there, `NSApplication::activate()`
     worked:
     - In one run the window watcher (15 ms sampling) saw the dev app become
       frontmost 33 ms after the call (`a13-actlat-watch.log`). That run read
       no key state before the next step.
     - In another run, `isActive && isKeyWindow` read true about 1 s after the
       call (`run1.log`, 121198470 → 121199552). The key transition itself was
       never timed.
     - **Not measured:** activating while the user is typing in another app,
       for example `make qa` run from a terminal. Since macOS 14,
       `activate()` is cooperative. See *Open Questions*.

     `NSApp.deactivate()` leaves a non-key state (`isActive` true,
     `isKeyWindow` false). `activate`, `set_focus` and `makeKeyAndOrderFront`
     did not fix it. What did was `hide`, then `unhideWithoutActivation`, then
     `activateIgnoringOtherApps` (`run1.log`, 121245692 → 121249288).
   - **An inactive app's menu still takes the key.** A post made while
     `isActive` was false still routed `guard`, straight to the menu with 0
     WebKit frames (`run1.log`, press 121189421).
   - **Stale stamp.** A keyDown stamped now − 2500 ms routed `terminate` with
     `age_ms=2569`: the stamp plus a 69 ms round trip. The process was gone
     68 ms after the handler ran, 125 ms after the post (`b1-stale-watch.log`).
   - **Hold override.** A temporary debug `held_until` was ORed into
     `on_poll`'s key read. The run gave `HideWindows` at +507 ms and
     `[StopPolling, Exit]` at +1007 ms, and the process was gone by +1109 ms.
     `key_state` read false throughout.
   - **Clock.** `ProcessInfo.systemUptime` equals `CLOCK_UPTIME_RAW` to the
     millisecond, so `now_ms()` is the right stamp. The spike stamped with
     `systemUptime`, which needed the `objc2-foundation` feature
     `NSProcessInfo`; stamping with `now_ms()` does not.
   - **Fade timing.** A tap's notice starts fading about 1035 ms after the
     poll that saw the release, and reaches alpha 0 about 250 ms after that
     (`a7-deact-watch.log`, `a8-act-watch.log`). `hide_notice` orders it out
     one 20 ms step later. That is `LINGER_MS` plus `FADE_MS` plus thread and
     hop latency.
   - **Cargo.** `NSEvent::keyEventWithType…` needs the `objc2-app-kit` feature
     `NSGraphicsContext`. `Cargo.lock` does not change.
3. **The driver serves one request at a time**, on purpose
   (`dev_driver.rs:241-244`, and the frontend queue at `devDriver.ts:74-76`).
   No verb can run while another is waiting.
4. **`DevRequest` is not conformance-gated.** It lives in
   `tarmac_protocol::dev`, separate from `Msg`. A new variant needs round-trip
   tests, not new hex vectors.

## Motivation

#179 needed a hand run (2026-09-19). Two of its rows, Q11 and Q12's busy half,
still lack log backing. Every release-path change will owe the same
run again. Q1's tap/hold/double tap across focus contexts, Q12's freshness
and Q16's re-show are mechanical. Holds, double taps and stale presses end the
app, which is exactly why nobody re-runs them casually. A verb plus a knockout
per scenario turns them into a repeatable check an agent can run unattended.
The rows that depend on the input source (Q11) stay manual, and the QA doc says
so.

## Proposed Solution

### Overview

A new debug-only verb, `tarmac dev quit-key [--hold <ms>] [--age <ms>]`:
1. makes the main window key, activating the app if needed;
2. reads the retargeted Quit item's live chord;
3. arms a debug-only hold override;
4. posts a constructed keyDown in-process;
5. reports the route the Quit handler took for it.

The snapshot's `quit_guard` gains the guard's `phase` and the notice's
`visible` and `alpha`, so `--until` can watch a tap, a re-show and a hide.

`make qa` gains the scenarios that leave the app running. A new
`make qa-quit CASE=<hold|double|stale>` runs one scenario that ends the app, so
it needs a fresh `make run` each time.

### Key Components

- **`core/crates/tarmac-protocol/src/lib.rs`, `dev` module.**
  - `DevRequest::QuitKey { hold_ms: Option<u32>, age_ms: Option<u32> }`,
    tagged `quit_key`; `None` fields are skipped.
  - `QUIT_KEY_BUDGET_MS`, `HOLD_MS_MAX` and `AGE_MS_MAX`, the only definitions of the verb's
    budget and ranges.
  - `DevRequest::timeout_ms()` returns `Some(QUIT_KEY_BUDGET_MS)` for it, so
    both ends of the socket wait long enough. Its doc comment ("Only `snapshot`
    waits on anything", `lib.rs:520-523`) is updated to name both.
- **`core/crates/tarmac-cli/src/dev.rs`.** `parse_verb` gains `quit-key` and
  its two flags, and `USAGE` gains the line.
- **`desktop/src/devDriver.ts`.**
  - `handle` answers `quit_key` before routing, like `snapshot`: it needs no
    card.
  - It calls `invoke("dev_quit_key", { holdMs, ageMs })` and replies with the
    command's JSON, or its error JSON.
  - `IMPLEMENTED` gains `quit_key`.
  - `quitGuardFacts`' type widens to the new `quit_guard` shape.
- **`desktop/src/kit/devSnapshot.ts`.** The `quitGuard` input type widens.
  `buildSnapshot` copies it, except that `notice.alpha` is reported as `0`
  whenever `notice.visible` is false. A hidden panel's own `alphaValue` is
  1.0, because `hide_notice` resets it, and that would read as a notice at full
  opacity. This is a decision, so it lives here, as `selection`'s `""` → `null`
  does.
- **`desktop/src-tauri/src/quit_intercept.rs`.** Everything below is
  `#[cfg(debug_assertions)]`.
  - `QuitIvars` gains `held_until_ms: Cell<u64>`. `on_poll`'s key read becomes
    `key_state(…) || now_ms() < held_until_ms`.
  - `QuitIvars` also gains `last_quit: Cell<Option<(u64, Route, i64)>>`,
    holding `press_ms`, route and `age_ms`. `on_quit` writes it after `route`
    and before applying it; `terminate:` never returns, so the write must come
    first.
  - `async fn dev_quit_key(app, hold_ms, age_ms) -> Result<Value, Value>`. It
    must never block the main thread: every AppKit step is a
    `run_on_main_thread` hop, and every wait is an async sleep between hops.
  - `dev_quit_guard` also reports `phase` and `notice`.
    - Until the first `ShowHud` the `notice` ivar is `None`, and
      `dev_quit_guard` reports it as `{ visible: false, alpha: 0 }`.
    - With no `QuitTarget` at all (`TARGET` is `None`, as under S37's
      knockout), it reports `phase: "idle"` and
      `notice: { visible: false, alpha: 0 }`. `retargeted: false` is what
      carries the meaning.
- **`desktop/src-tauri/src/quit_guard.rs`.**
  - `Route` gains `Clone, Copy`. It has two unit variants, and `Cell::get`
    needs `Copy`.
  - `pub fn phase_name(&Phase) -> &'static str` returns `"idle"`, `"showing"`
    or `"confirming"`.
  - `pub fn press_matches(recorded_ms: u64, stamp_ms: u64) -> bool` is true
    when `recorded_ms` is `stamp_ms` or `stamp_ms − 1`. It is the tolerance in
    Data Flow step 4.4, and it is a decision, so it lives here and is tested
    (S6).
  - Only tests and debug-only code read `phase()`, `phase_name` or
    `press_matches`, so all three carry
    `cfg_attr(not(any(test, debug_assertions)), allow(dead_code))`. `phase()`
    carries `cfg_attr(not(test), …)` today.
- **`desktop/src-tauri/src/quit_notice.rs`.** `Notice` gains `is_visible()`
  and `alpha()` (the panel's `isVisible` and `alphaValue`), read on the main
  thread.
- **`desktop/src-tauri/src/lib.rs`.** `dev_quit_key` joins the
  `#[cfg(debug_assertions)]` command list beside `dev_quit_guard`.
- **`desktop/src-tauri/Cargo.toml`.** `objc2-app-kit` gains the feature
  `NSGraphicsContext`, the type of `keyEventWithType…`'s `context` argument.
  The features there carry no per-line comments; the block comment above the
  dependency covers them.
- **`scripts/qa/smoke.mjs`.** D12–D15 are added. The header comment gains them.
- **`scripts/qa/quit.mjs`** (new) and a **`Makefile`** target `qa-quit`, pinned
  to this worktree's `.dev/tarmac-dev.sock` as `qa` is. The script runs one
  `CASE`, then asserts that the app is gone.
- **Docs.**
  - `.claude/skills/dev-channel/SKILL.md`: the verb list, the new snapshot
    fields, and `make qa-quit`.
  - `desktop/qa/hold-to-quit-qa.md`: which rows the driver now covers, which
    stay manual, and the knockout runs.
  - `desktop/qa/qa-driver-qa.md`: the new `D` scenarios' results.
  - `docs/architecture.md`, the `tarmac dev` paragraph: one sentence naming
    `quit-key`.

### Data Flow

1. **CLI.** `tarmac dev quit-key --hold 1000` becomes
   `QuitKey { hold_ms: Some(1000), age_ms: None }`. The read deadline is
   `read_deadline_ms(Some(QUIT_KEY_BUDGET_MS))`.
2. **Backend relay.** `DevDriver::dispatch` emits `dev-request` as for every
   verb. Its budget is `QUIT_KEY_BUDGET_MS + BACKEND_SLACK_MS`.
3. **Frontend.** `handle` sees `t === "quit_key"` and awaits
   `invoke("dev_quit_key", …)`. The page's event loop stays free while it
   waits, which is what lets the page receive the posted key.
4. **`dev_quit_key`**, with `hold = hold_ms.unwrap_or(TAP_HOLD_MS)` and
   `age = age_ms.unwrap_or(0)`.

   The command re-checks the ranges, because the wire accepts any `u32`. A
   `hold` outside `1..=10000` or an `age` above `60000` replies error
   `bad_request` without posting.

   The bounds are defined once, as `HOLD_MS_MAX = 10_000` and
   `AGE_MS_MAX = 60_000` in `tarmac_protocol::dev`. The CLI and the command
   both use them, so S20 and S30 pin the only definition. The app-side
   re-check itself is untested defensive wiring.

   Two failures can happen in any step. If `TARGET` is `None`, the reply is
   error `not_retargeted`: with no target, nothing guards Quit. If
   `run_on_main_thread` returns `Err`, the event loop is exiting, and the
   reply is error `app_unresponsive`.
   1. **On main: find the item.** Walk the menu for the item whose action is
      `tarmacQuit:` and whose target is live, and check that no native
      `terminate:` item remains. That is the predicate `is_retargeted` applies,
      so `quit_guard.retargeted` and this step cannot disagree. If it fails,
      reply error `not_retargeted`. **Posting then would reach native
      `terminate:` and quit for real.**
   2. **Make the window key.** If the main window is not key, call
      `NSApplication::activate()`. Poll `isActive && isKeyWindow` with hops
      every ~20 ms. If it is still false after `KEY_WAIT_MS`, reply error
      `not_key`. **Never call `deactivate`.**

      Start the step with a single "already key?" test (one line), so that
      S21's knockout can short-circuit it.
   3. **On main: arm the override and post.**
      - Read the item's `keyEquivalent` and `keyEquivalentModifierMask`.
      - Take `now = now_ms()`, set `held_until_ms = now + hold`, and set
        `stamp_ms = now.saturating_sub(age)`.
      - Set `last_quit` to `None`. With `--age`, a new stamp can equal an
        earlier press's `press_ms`. A stale record would then be reported
        as this press's route.
      - Build a keyDown with the item's characters, both as characters and
        as characters-ignoring-modifiers, and the item's mask. Set keyCode 12,
        `isARepeat` false, the main window's `windowNumber`, and a timestamp
        of `stamp_ms / 1000` seconds.
      - Post it with `postEvent_atStart(false)`.
   4. **Wait for the handler.** Hop about every 20 ms, for at most
      `ROUTE_WAIT_MS`, until `last_quit` holds a press for which
      `press_matches(press_ms, stamp_ms)` is true.

      The match allows 1 ms because `on_quit` reads the press back as
      `quit_guard::press_ms(timestamp)`, which truncates, and
      `(x / 1000.0) * 1000.0` can land just under `x`. For example,
      `press_ms(1001.0 / 1000.0)` is `1000`.

      It happens whenever `stamp_ms / 1000` lies just above a power of two,
      in bands covering about 2.4 % of each doubling of uptime. Inside a band,
      roughly a quarter of stamps truncate. For example, 754,974 of the
      integers from 131,072,001 to 134,217,724 ms do (about 36–37 h of
      uptime). An exact match would read such a press as swallowed.
      - If found, reply `{ "route": "guard", "age_ms": …, "press_ms": …,
        "hold_ms": … }`, with `route`, `age_ms` and `press_ms` as the handler
        recorded them.
      - If not found, reset `held_until_ms` to 0, then reply
        `{ "route": null, "press_ms": …, "hold_ms": … }`, with `press_ms` =
        `stamp_ms`. Left armed, the override would make a real ⌘Q tap within
        the hold window read as held and quit the app.
        `route: null` is an observation, not an error: something on the way
        swallowed the key.
      - A press that routes `terminate` never replies, because the app
        terminates inside the handler. The CLI then prints
        `tarmac: app connection lost: …` and exits 1.
   5. **Afterwards, post the keyUp.** A spawned task captures the
      `held_until_ms` value that step 3 set. It does not re-read it, so the
      reset in step 4.4 does not move it. The task sleeps until that value,
      then posts the matching keyUp on main. By then the app may already be
      exiting; any hop failure is ignored.
5. **Release is the override expiring.** The poller reads `held` until
   `held_until_ms` passes, so the guard sees an ordinary release. The tap
   default of 100 ms means a double tap quits on release, as a real one does,
   not on the first poll.
6. **Snapshot.** `dev_quit_guard` returns
   `{ retargeted, enabled, phase, notice: { visible, alpha } }`, read on the
   main thread in the same hop. `alpha` is the panel's raw `alphaValue`.
   `buildSnapshot` zeroes it when `visible` is false.

### Interface Contract

```text
tarmac dev quit-key [--hold <ms>] [--age <ms>]
  --hold  1..=10000, default 100 (TAP_HOLD_MS)   how long the key reads as held
  --age   0..=60000, default 0                    stamp the press this many ms in the past
  usage errors (exit 2): missing value, not an integer, out of range,
  repeated flag, unknown flag, positional argument — the message's first
  line names the flag or argument (`usage()` appends the full USAGE after it)
```

```rust
// tarmac_protocol::dev
DevRequest::QuitKey {
    #[serde(default, skip_serializing_if = "Option::is_none")] hold_ms: Option<u32>,
    #[serde(default, skip_serializing_if = "Option::is_none")] age_ms: Option<u32>,
}                                  // wire tag "quit_key"
pub const QUIT_KEY_BUDGET_MS: u32 = 3_000;   // DevRequest::timeout_ms() for QuitKey
pub const HOLD_MS_MAX: u32 = 10_000;         // --hold is 1..=HOLD_MS_MAX
pub const AGE_MS_MAX: u32 = 60_000;          // --age is 0..=AGE_MS_MAX

// quit_guard.rs
pub fn phase_name(phase: &Phase) -> &'static str;   // "idle" | "showing" | "confirming"
pub fn press_matches(recorded_ms: u64, stamp_ms: u64) -> bool;   // stamp.checked_sub(recorded) ∈ {Some(0), Some(1)}
```

Reply bodies (`ok: true`):

```json
{ "route": "guard", "age_ms": 17, "press_ms": 121268655, "hold_ms": 100 }
{ "route": null, "press_ms": 121268655, "hold_ms": 100 }
```

Errors (`ok: false`, the 2609.0015 JSON error shape): `not_retargeted` and
`not_key` are new. Two existing codes gain a meaning:
- `bad_request` now also covers a `hold` or `age` out of range on the wire;
- `app_unresponsive` now also covers a failed main-thread hop.

`app_not_ready` is unchanged. An app built
before this verb answers `unsupported_verb`, through its existing
`#[serde(other)]` path.

The verb does not read the toggle. With *Warn Before Quitting* off, the press
routes `terminate` and the app quits with no reply, as Q3 requires of a real
tap. The scenario scripts check `enabled == true` before they press.

Snapshot (`v` stays 1; additive keys inside `quit_guard`):

```ts
quit_guard: {
  retargeted: boolean;
  enabled: boolean;
  phase: "idle" | "showing" | "confirming";
  notice: { visible: boolean; alpha: number };   // alpha 0 when not visible (buildSnapshot)
} | null
```

Constants, all in `quit_intercept.rs` unless noted: `TAP_HOLD_MS = 100`,
`KEY_WAIT_MS = 1000`, `ROUTE_WAIT_MS = 1000`; and, in `tarmac-protocol`,
`QUIT_KEY_BUDGET_MS = 3000`, `HOLD_MS_MAX = 10_000`, `AGE_MS_MAX = 60_000`. `3000 ≥ KEY_WAIT_MS + ROUTE_WAIT_MS` plus hops.

**Release builds.** None of the verb, the command, the override or `last_quit`
exists in a release build. The CLI side keeps 2609.0015's gate: a release
binary still exits 1 with `driver unavailable in release builds`.

## Alternatives Considered

### Post from outside with `CGEventPostToPid`

Spike 1 shows it works for taps, and it would need no app code for posting. It
was not chosen for three reasons:
- the std-only CLI would need CoreGraphics FFI;
- it still cannot drive a hold, because `key_state` never reads a posted key;
- it cannot choose the key window. An in-process post can, and that choice is
  what decides whether the page sees the key.

### A debug switch that suppresses `Exit`

This would let the quitting cases join `make qa` without ending the app. It
was rejected because it forks the behaviour under test: the scenario would pass
while `app.exit(0)` from the timer callback broke. A separate entry point that
lets the app really quit keeps the check honest.

### Drive holds with a real key-state source

No event source makes `key_state` read a posted key as down (spike 1). The
remaining global `CGEventPost` taps reach the frontmost app, and a ⌘Q there
could quit the user's other apps. The override is the only safe route. It tests
everything except the physical key read, which Q8 and the hand-run rows keep
covering.

### Extend `focus` to doc cards in this spec

Spike 2 focused markdown and HTML cards with posted mouse clicks. That is
`focus`'s contract, not the guard's. It is left as a follow-up (see *Open
Questions*), and Q1's two doc-card cells stay manual.

## Security Considerations

- **Debug builds only.** The verb, the command, the override and `last_quit`
  are compiled out of release builds, and a release build is checked for the
  command's name (S34).
- **Nothing leaves the process.** Posting stays in-process (`postEvent`), so
  no event can reach another app. That is the reason `CGEventPostToPid` and
  `CGEventPost` were not used.
- **The verb activates the app**, taking focus from whatever the user was
  using. That is the price of taking the real path, and it is stated in
  `SKILL.md`.

## Acceptance Scenarios

Automated scenarios name their test home. Rust tests go in the file's
`#[cfg(test)] mod tests`, and TS tests go beside the module. `[D]` scenarios run
against a live `make run` app, in `make qa` or `make qa-quit`. Each `[D]`
scenario names a **knockout**, a one-line temporary change that must make it
fail. Record each knockout run in `desktop/qa/hold-to-quit-qa.md`, and revert
the line, never the file.

**Before every press.** Each `quit-key` in D12–D15 and in `quit.mjs` is preceded
by these checks. If one fails, the script fails without pressing.
- `quit_guard.enabled == true` and `quit_guard.retargeted == true`. The toggle
  belongs to the user (`.dev/app-prefs.json`, and D11 asserts only its type).
  With it off, a tap routes `terminate` and ends the app mid-run, and S16 would
  pass for the wrong reason.
- `--until 'quit_guard.phase == "idle"'`, then
  `--until 'quit_guard.notice.visible == false'`, each with `--timeout 3000`.
  A press within 1 s of the previous press is a second tap, and it would quit
  the app in the middle of `make qa`. A notice is never gone sooner than
  1200 ms (`LINGER_MS` + `FADE_MS`) after the release that ended its tap, so
  once it is gone, more than 1 s has passed since the press.
- At least 1000 ms since the previous `quit-key` reply arrived. This is the
  backstop for when no notice was shown at all: under S10's knockout, for
  example, "notice gone" says nothing about elapsed time.

The deliberate second presses in S13 and S15 are the only exceptions.

**Timing.** A script cannot read the app's clock. Times below are measured from
when a reply *arrives*, which is shortly after its press: in spike 2 the
handler ran 1–111 ms after the post. `press_ms` differences come from the
replies themselves.

### Happy Path

- **S1** (`tarmac-cli/src/dev.rs`) — Given the argument vectors below, `parse_verb` returns:
  - `["quit-key"]` → `QuitKey { hold_ms: None, age_ms: None }`;
  - `["quit-key", "--hold", "1000"]` → `hold_ms: Some(1000)`;
  - `["quit-key", "--age", "2500"]` → `age_ms: Some(2500)`;
  - `["quit-key", "--age", "0", "--hold", "10000"]` → `Some(10000)` and `Some(0)`.
- **S2** (`tarmac-protocol`) — Given each `QuitKey` value from S1, when encoded and decoded, then it
  round-trips. Its tag is `"quit_key"`. With both fields `None`, the encoded map carries only `t`.

  Red first: add the variant as a bare stub, without `skip_serializing_if`, so the test compiles.
  The "only `t`" assertion then fails, because `None` encodes as nil. The round-trip and tag
  assertions pin the derive; like S5's first case, they cannot be observed red.
- **S3** (`tarmac-protocol`) — `DevRequest::QuitKey { .. }.timeout_ms()` is `Some(3000)`, and
  `Snapshot { timeout_ms: None, .. }` is still `None`. (`tarmac-cli/src/dev.rs`) —
  `read_deadline_ms` of a parsed `quit-key` request's `timeout_ms()` is `6000`.
- **S4** (`quit_guard.rs`) — `phase_name` returns `"idle"` for `Idle { last_start_ms: None }` and
  for `Idle { last_start_ms: Some(1) }`, `"showing"` for `Showing { started_ms: 1 }`, and
  `"confirming"` for `Confirming`.
- **S5** (`kit/devSnapshot.test.ts`) — `buildSnapshot`:
  - Given
    `quitGuard: { retargeted: true, enabled: true, phase: "showing", notice: { visible: true, alpha: 0.5 } }`,
    `quit_guard` deep-equals that object.
  - Given the same input with `phase: "idle"` and `notice: { visible: false, alpha: 1 }` (a hidden
    panel, whose `alphaValue` `hide_notice` reset to 1), `quit_guard.notice` is
    `{ visible: false, alpha: 0 }`.
  - `quitGuard: null` still gives `quit_guard: null`.

  The second case is the red one. `buildSnapshot` copies `quitGuard` verbatim today, so under
  Vitest the first case already passes; before the change it fails only `tsc`, which §1 does not
  count as red.
- **S6** (`quit_guard.rs`) — `press_matches`:
  - `(1001, 1001)` → `true`;
  - `(1000, 1001)` → `true` (the truncation case: assert alongside that `press_ms(1.001)` is
    `1000`);
  - `(999, 1001)` → `false` (an earlier press);
  - `(1002, 1001)` → `false`;
  - `(0, 0)` → `true`, and `(u64::MAX, 0)` → `false`: no overflow at either end.
- **S10 [D, `make qa` D12] tap from a plain-shell terminal.**
  - **Given** zoom 1 and the terminal focused (`reset`), with its `scrollback_tail` recorded;
  - **when** `quit-key --hold 300` runs. That is still a tap, since 300 < `HOLD_MS` (500). At the
    default 100 ms, `showing` ends within about 100 ms of the reply arriving (the release is
    polled at roughly press + 110–130 ms), which is too soon for a separate `snapshot` to catch
    reliably;
  - **then** it exits 0 with `route == "guard"`, `hold_ms == 300` and `0 <= age_ms <= 2000` (Q12's
    bound, on an idle page);
  - `snapshot --until 'quit_guard.phase == "showing"' --timeout 1000` and then
    `--until 'quit_guard.notice.visible == true' --timeout 1000` both hold;
  - `--until 'quit_guard.phase == "idle"' --timeout 3000` and then
    `--until 'quit_guard.notice.visible == false' --timeout 3000` both hold;
  - the app still answers, and the terminal's `scrollback_tail` equals the recorded one. A stray
    `q` would echo at the prompt.
  - If the reply is error `not_key`, D12 fails and prints that the activation was refused from
    this posture (see *Open Questions*). It does not retry.

  *Knockout:* in `apply`, make `Effect::ShowHud` do nothing. `notice.visible` never becomes true.
- **S11 [D, D13] tap through a kitty terminal.**
  - **Given** the terminal focused, with kitty flags 5 pushed by
    `type <t> "printf '\033[>5u' && echo <sentinel>-$((6*7))\n"` and the push confirmed by
    `--until 'cards[<t>].term.scrollback_tail contains "<sentinel>-42"'`. Only the command's output
    contains `-42`, so the typed line's own echo cannot satisfy the wait. Flags 5 is what Claude
    Code pushes, and this is #171's headline case.
  - **When** `quit-key` runs, **then** `route == "guard"` and
    `--until 'quit_guard.notice.visible == true' --timeout 1000` holds.
  - Afterwards, a `finally` step pops the flags. It also runs when the scenario fails.
    - First it sends `type <t> "\n"`. Under the knockout, xterm's `ESC[113;9u` lands at the
      prompt. Enter is still a legacy `\r` under flags 5, so it flushes that junk as its own
      failing command.
    - `ctrl+u` would not clear the line. Under flags 5, xterm encodes it as `ESC[117;5u`.
    - Then it sends `printf '\033[<u' && echo <sentinel2>-$((6*7))` and waits on
      `"<sentinel2>-42"`. zsh echoes the typed line, so only the command's output contains `-42`,
      and the `&&` means that output appears only if the pop ran.
    - Pushed flags would otherwise leak into every later run, because the daemon replays the
      scrollback.

  *Knockout:* disable the kitty rule in `kit/termKeyRoute.ts`: prefix `xtermHandlesKey`'s
  `input.meta && input.kittyFlags > 0` condition with `false &&`, as the spike's
  `knockout-termKeyRoute.patch` does. The reply is `route: null`, and no notice appears.
- **S12 [D, D14] tap from the board.** Given `focus board`, when `quit-key` runs, then
  `route == "guard"` and `--until 'quit_guard.notice.visible == true' --timeout 1000` holds.

  *Knockout:* S10's.
- **S13 [D, D15] re-show (Q16).**
  - **Given** the plain-shell terminal focused (Q16's posture) and a first `quit-key` that routed
    `guard`;
  - **when** `snapshot --until 'quit_guard.notice.alpha != 1' --timeout 3000` holds, and the
    snapshot it returns reads `quit_guard.notice.visible == true` (the first notice is fading, not
    gone), and a second `quit-key` runs at once. The grammar has no `<`; the notice sits at
    exactly 1 until its first fade step;
  - **then:**
    - `press_ms₂ − press_ms₁` is above 1000, so this is not a second tap. It is also below 1300,
      the earliest the first notice can be gone (release ≥ press + 100, then `LINGER_MS` and
      `FADE_MS`); the spike measured about 1400. Outside that window the run fails as
      inconclusive (re-run it) rather than passing, because a second press after the first
      notice is gone makes the knockout vacuous;
    - `--until 'quit_guard.notice.alpha == 1' --timeout 300` holds;
    - `--until 'quit_guard.notice.visible == false' --timeout 3000` holds, and no sooner than
      900 ms after reply₂ arrived. The second notice lingers 1000 ms past its own release;
    - the app still answers.

  *Knockout:* remove the `hud_gen` bump in `show_notice`. The first notice's fade keeps stepping
  on the new notice and orders it out a few hundred ms after the second press, so
  `visible == false` holds well before 900 ms.
- **S14 [D, `make qa-quit CASE=hold`] a hold hides, then quits on release.**
  - **Given** a fresh `make run`;
  - **when** `quit-key --hold 2000` runs;
  - **then** `route == "guard"`;
  - `--until 'quit_guard.phase == "confirming"' --timeout 1000` holds (the hide lands at about
    +500 ms), and the snapshot it returns reads `quit_guard.notice.visible == true`;
  - `--until 'visibility == "hidden"' --timeout 1000` holds;
  - a `snapshot` started 1500 ms after the reply arrived still exits 0: the window is gone, the key
    still reads held, and the app has not quit;
  - by 3500 ms after the reply arrived, `snapshot` exits 1 with `no tarmac app driver`.

  The hold is 2000 ms, not Q1's ~1 s, because `visibility` lags the hide. The lag was measured
  under 0.5 s for `NSApp.hide`/`unhide` cycles (`desktop/qa/qa-driver-qa.md`, stage 2), but not
  for `window.hide()`. At a 1 s hold the app exits about 500 ms after the hide. If `visibility`
  never flips for `window.hide()`, stop and report it: the hide then has no driver-visible fact,
  and adding one changes the contract.

  *Knockout:* drop `|| now_ms() < held_until_ms` from `on_poll`. The hold ends as a tap,
  `confirming` never holds, and the app stays.
- **S15 [D, `CASE=double`] two taps within 1 s quit on release.**
  - **Given** a fresh app and a first `quit-key`, followed by
    `--until 'quit_guard.phase == "idle"' --timeout 1000`. The first tap's release must already
    have been polled, so the second press takes `Idle`'s `recent` branch and not `Showing`'s
    re-press branch, which ignores `recent`;
  - **when** a second `quit-key --hold 400` runs, with `press_ms₂ − press_ms₁ < 1000` from the two
    replies;
  - **then** the second reply is `route == "guard"`, and
    `--until 'quit_guard.phase == "confirming"' --timeout 300` holds;
  - by 2000 ms after the second reply arrived, `snapshot` exits 1 with `no tarmac app driver`.

  The second hold is 400 ms: under `HOLD_MS`, so the press is still a tap, and long enough that
  `confirming` outlives a snapshot round trip before `Exit`. At 100 ms the app exits 100–150 ms
  after the press. `visibility` is not asserted here. It lags the hide by up to ~0.5 s, which is
  longer than this press lasts, and S14 covers `HideWindows`.

  *Knockout:* make the `recent` test in `on_quit_key` always false. The second press shows the
  notice instead, releases before `HOLD_MS`, `confirming` never holds, and the app stays.
- **S16 [D, `CASE=stale`] a stale press quits at once.**
  - **Given** a fresh app;
  - **when** `quit-key --age 2500 --hold 1` runs;
  - **then** it exits 1 with stderr `tarmac: app connection lost: …`, and within 1000 ms of that
    exit `snapshot` exits 1 with `no tarmac app driver`;
  - `--hold 1` keeps the knockout deterministic.
    - With the default hold, a backdated press that routed `guard` would read as held on its
      first poll. By then it is about 2000 ms past the hold threshold, so it would commit and quit.
    - Whether the reply beat that `Exit` would depend on timing, so the knockout's result would
      too. And "the app stays" would be false.
  - *recorded, not asserted by `quit.mjs`:* the last `quit-key` line of the `make run` output reads
    `route=terminate` and `age_ms >= 2500`. `quit.mjs` cannot see that terminal, so whoever started
    `make run` records the line in `hold-to-quit-qa.md`.

  *Knockout:* raise `FRESHNESS_BOUND_MS` to `10_000`. The reply is `route == "guard"` (exit 0). The
  first poll lands after the 1 ms override has expired, so it reads a release, the press ends as a
  tap, and the app stays.

### Edge Cases

- **S20** (`tarmac-cli`) — `["quit-key", "--hold", "1"]` and `["quit-key", "--age", "60000"]`
  parse. These are the bounds.
- **S21 [D, part of D12]** — the verb activates a non-frontmost app.
  - **Given**, before D12 presses, the frontmost app's pid
    (`lsappinfo info -only pid "$(lsappinfo front)"`; `lsappinfo front` prints an ASN, not a pid)
    and the dev app's pid (`lsof -t` on `.dev/tarmac-dev.sock`, the way `make kill-daemon` finds
    its daemon). When `make qa` runs from another app's terminal, the two differ.
  - **When** D12's `quit-key` replies, **then** the frontmost pid is the dev app's. Read the
    frontmost pid only after D12's `phase`/`visible` waits: `showing` lasts about 150 ms, and
    the `lsappinfo`/`lsof` calls must not eat into it. `lsappinfo info -only pid` prints
    `"pid"=637`; take the digits after `=`.
  - If the dev app was already frontmost before D12 (for example, `make qa` run from the dev app's
    own terminal), S21 is reported as not exercised, never as passed.

  *Knockout:* make step 2's "already key?" test always true (one line). The post then goes
  straight to an inactive app's menu, where it still routes `"guard"` (spike 2), so only this
  frontmost check catches it.
- **S22 [QA]** — with the window made non-key by clicking another app, `quit-key` activates the
  app, replies `route == "guard"`, and the handler's path goes through the page. Check this the
  way the spike did: a temporary backtrace line, reverted afterwards.

### Error Scenarios

- **S30** (`tarmac-cli/src/dev.rs`) — `parse_verb` returns `Err` for each of these. Assert on the
  message's **first line only**. `usage()` appends the whole `USAGE` text, which names both flags,
  so a whole-message check would pass even for the wrong flag. The first line names the flag
  (`--hold` or `--age`) for a missing, non-integer, out-of-range or repeated value, and names the
  token itself for an unknown flag or a positional argument:
  - `["quit-key", "--hold"]`, `["quit-key", "--hold", "0"]`, `["quit-key", "--hold", "10001"]`,
    `["quit-key", "--hold", "x"]`;
  - `["quit-key", "--age", "-1"]`, `["quit-key", "--age", "60001"]`;
  - `["quit-key", "--now"]`, `["quit-key", "extra"]`;
  - `["quit-key", "--hold", "5", "--hold", "6"]` (names `--hold`).

  `run` already maps every `Err` to exit 2 (2609.0015 S57), so the unit test needs no binary.
- **S31** (`tarmac-protocol`) — forward compatibility, following S47. This is a derive-pinned
  regression test: it cannot be observed red once the variant exists. A `quit_key` frame that also
  carries an unknown key (`{ t: "quit_key", hold_ms: 1, busy_ms: 5 }`) decodes to
  `QuitKey { hold_ms: Some(1), age_ms: None }`. Backward compatibility needs no new test. An app
  built before this variant answers `unsupported_verb` through `#[serde(other)]`, which
  `unknown_request_types_decode_to_unknown` already pins with `teleport`. Adding `quit_key` to that
  test would fail, because this build knows the tag.
- **S32 [QA, S37-style knockout]** — with `quit_intercept::install(…)` commented out in `lib.rs`,
  `quit-key` exits 1 with error `not_retargeted`, and **the app stays up**. Posting would have
  reached native `terminate:`.
- **S33 [QA]** — with the window minimised, `quit-key` exits 1 with error `not_key` after about
  `KEY_WAIT_MS` plus the hops, and the app stays up. The spike did not measure whether
  `activate()` un-minimises a window; this row expects it not to. If the window does become key,
  record that, then exercise `not_key` another way: with the app inactive, comment out the
  `activate()` call (revert the line afterwards). The verb must reply `not_key` without posting.
- **S34 [QA]** — `make bundle`, then `strings` on the release binary. It contains no
  `dev_quit_key` (the same check Q8 ran for `dev_quit_guard`).

## For the Implementing Agent

> **Your job:** make every acceptance scenario above pass with tests that would *fail if the behavior were wrong*. A green suite that passes for the wrong reason does not satisfy this contract — `/verify` will hunt for vacuous tests by asking, of each behavior, "what is the smallest change that breaks this, and would any test catch it?"

Work in `../tarmac-worktrees/183-quit-guard-qa-driver` on `feat/183-quit-guard-qa-driver`.
Follow `docs/coding-style.md` §1 and observe red first.

- **Pure, TDD:**
  - S1, S3's `read_deadline_ms` half, S20 and S30 in `tarmac-cli/src/dev.rs`;
  - S2, S3 and S31 in `tarmac-protocol`;
  - S4 and S6 in `quit_guard.rs`;
  - S5 in `kit/devSnapshot.test.ts`. Its hidden-notice case is the red one (see S5).
- **Thin wiring (exception 2):**
  - `dev_quit_key`, the override, `last_quit`, `Route`'s `Clone, Copy` and the `Notice` getters;
  - the `dev_quit_guard` fields;
  - `devDriver.ts`'s branch.

  They are discharged by the `[D]` scenarios, and each `[D]` scenario's knockout is part of that
  discharge. Run each knockout once, record it, and revert the line.
- **`[D]` scenarios.**
  - S10–S13 go into `scripts/qa/smoke.mjs` as D12–D15. S21's frontmost check folds into D12.
  - S14–S16 go into `scripts/qa/quit.mjs`, one `CASE` per run. `make qa-quit` defaults `CASE` to
    `hold`. Each case needs a fresh `make run`; restart it between cases.
  - Every press follows *Before every press* (the head of *Acceptance Scenarios*).
- **Hard rules:**
  - `dev_quit_key` never blocks the main thread. It is an `async` command; every AppKit call is a
    `run_on_main_thread` hop; every wait is an async sleep.
  - **Never post when no live `tarmacQuit:` item exists, or while a native `terminate:` item
    remains.**
  - Never call `NSApp.deactivate()`.
  - Never `CGEventPost` or `CGEventPostToPid`.
  - Never query `CGEventSourceKeyState(.privateState, …)`.
  - Never `pkill`. Stop a dev app by pid, and the daemon with `make kill-daemon`.
- **Don'ts:**
  - Don't `cargo fmt`.
  - Don't touch `Msg`, `docs/protocol.md` or the V1–V13 vectors.
  - Add no crate versions to `Cargo.lock`: only the `NSGraphicsContext` feature.
  - Serialize heavy builds while a dev app is up.

Test principles: assert on returned values, replies and snapshot fields, never on private state.
Write every expected value out literally. `[D]` scenarios wait with `--until`, never with a
fixed sleep. There are three exceptions:
- S14's probe, 1500 ms after its reply;
- the 1000 ms spacing in *Before every press*;
- the "app is gone" checks in S14–S16. `--until` cannot express those, because the app is not
  there to evaluate it. They poll `snapshot` about every 100 ms until it exits 1 with
  `no tarmac app driver`, up to the scenario's bound, and fail if the bound passes first.

`--until` supports only `==`, `!=`, `~=` (within 1) and `contains`, one comparison per
expression (`kit/devUntil.ts`). To check several fields at one moment, read them off the
snapshot that `--until` returns.

## Definition of Done

Done is when `/verify` passes against this spec:

- [ ] `make test` is green.
- [ ] Every automated scenario (S1–S6, S20, S30, S31) maps to at least one test. Every `[D]`
  scenario (S10–S16, S21) has been run against a live app, and its result and knockout run are
  recorded. S22, S32–S34 are recorded in `desktop/qa/hold-to-quit-qa.md`.
- [ ] No covered-but-vacuous scenarios: each automated test fails under the smallest break of
  its behaviour, and each `[D]` scenario fails under its knockout.
- [ ] Tests meet the Desiderata bar, with no AP-1…AP-8 violations.
- [ ] No implementation-quality blockers. `make docs-check` is green. `SKILL.md`, the two QA docs
  and `architecture.md` are updated. `hold-to-quit-qa.md` names which Q rows are now
  driver-covered and which stay manual.

## Trade-offs and Limitations

- **Not a real key.**
  - The input source and IME play no part, because the characters are supplied. Q11 stays manual.
  - The physical `key_state` read is bypassed by the override. Q8 and the hand-run Q1 hold keep
    covering it.
  - keyCode is fixed at 12. Only the poller reads it, and the override makes its value moot.
  - `--age` backdates the press for the whole gesture, not only for freshness. The hold threshold
    counts from `press_ms` (`sampled − started_ms ≥ HOLD_MS`), so an `--age` plus `--hold` that
    reaches about 500 ms can commit a hold and quit. `--age 450` with the default hold can.
- **The verb steals focus** when the window is not key. That is the only way the key takes the
  page's path.
- **Each quitting case costs a relaunch.** Hold, double tap and stale each end the app, so
  `make qa-quit` runs one case per `make run`.
- **Q12's busy half is not covered.** The driver serves one request at a time, so nothing can
  freeze the page while `quit-key` runs. See *Open Questions*.
- **Doc-card focus contexts are not covered** (see *Alternatives Considered*).
- **A stale press gets no reply.** The app terminates inside the handler, so S16 asserts the
  exit. The route is recorded from the `make run` output, which `quit.mjs` cannot see.

## Open Questions

These are for the gate:

- [ ] **Quitting cases in a separate `make qa-quit`,** one case per fresh app (recommended). The
  alternatives are a debug exit-suppress switch (rejected above) or leaving them manual.
- [ ] **The verb activates the app when its window is not key** (recommended). The alternative
  is to refuse with `not_key` and make the operator click the window. Under the alternative,
  step 2 replies `not_key` at once, S21 is dropped, and `make qa` needs the dev window key before
  D12, so it cannot run unattended from another app's terminal.
  **Unmeasured risk:** the spike activated from a hidden-then-unhidden posture. It never tried
  while the user was typing in another app. Since macOS 14, `activate()` is cooperative, and the
  frontmost app may not yield. The first implementation step measures this. If activation is
  refused, D12 reports `not_key` (S10), and this question falls back to the alternative.
- [ ] **Doc-card focus** (Q1's markdown and HTML cells): a follow-up that extends `focus` to doc
  cards with posted clicks, or keep them manual?
- [ ] **Q12's busy half:** a later `quit-key --busy <ms>` could have the frontend schedule a busy
  loop before invoking the command. It is not measured. Defer it, or measure it and add it here?

## References

- Issue #183; spikes in the main checkout at `.dev/postpid-spike/` and `.dev/inapp-spike-183/`.
- Specs 2609.0015 (driver) and 2609.0016 (guard, with the #179 corrections).
- `desktop/qa/hold-to-quit-qa.md` (Q1, Q8, Q11, Q12, Q16, S37).
